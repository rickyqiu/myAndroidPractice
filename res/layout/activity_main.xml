<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/urlCaption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="please input url:" />

    <TextView
        android:id="@+id/httpContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/httpCode"
        android:layout_below="@+id/httpCode"
        android:layout_marginTop="15dp"
        android:maxLines = "1000"
		android:scrollbars = "vertical"
        android:text="http content" />

    <EditText
        android:id="@+id/urlInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/urlCaption"
        android:layout_below="@+id/urlCaption"
        android:ems="10" />

    <TextView
        android:id="@+id/httpCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/urlInput"
        android:layout_below="@+id/urlInput"
        android:layout_marginTop="18dp"
        android:text="http code" />

    <Button
        android:id="@+id/goBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/urlInput"
        android:layout_alignBottom="@+id/urlInput"
        android:layout_toRightOf="@+id/urlInput"
        android:text="Go" />

</RelativeLayout>